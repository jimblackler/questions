// Create a list
l = [1, 2, 3];

// Add an item
l.push(4);

// Get length of the list
console.log(l.length);

// Return the second item
console.log(l[1]);

// Remove the first item
l.splice(0, 1);

// Clear the list
l.length = 0;